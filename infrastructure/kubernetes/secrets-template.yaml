# IMPORTANT: This is a template file. DO NOT commit actual secrets to git.
# Create actual secrets using: kubectl create secret generic <name> --from-literal=key=value

# Example commands to create secrets:
#
# kubectl create secret generic backend-secret \
#   --from-literal=SECRET_KEY='your-secret-key-here' \
#   --from-literal=DB_PASSWORD='your-db-password' \
#   --from-literal=EMAIL_HOST_PASSWORD='your-email-password' \
#   --from-literal=AWS_ACCESS_KEY_ID='your-aws-key' \
#   --from-literal=AWS_SECRET_ACCESS_KEY='your-aws-secret' \
#   --from-literal=SENTRY_DSN='your-sentry-dsn' \
#   -n bugstracker-production
#
# kubectl create secret generic postgres-secret \
#   --from-literal=database='bugstracker' \
#   --from-literal=username='postgres' \
#   --from-literal=password='your-postgres-password' \
#   -n bugstracker-production
#
# kubectl create secret generic redis-secret \
#   --from-literal=password='your-redis-password' \
#   -n bugstracker-production
#
# kubectl create secret generic flower-secret \
#   --from-literal=username='admin' \
#   --from-literal=password='your-flower-password' \
#   -n bugstracker-production
#
# kubectl create secret tls bugstracker-tls \
#   --cert=path/to/tls.crt \
#   --key=path/to/tls.key \
#   -n bugstracker-production

---
# Template for backend-secret (DO NOT FILL IN - use kubectl create secret instead)
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: bugstracker-production
type: Opaque
stringData:
  SECRET_KEY: ""  # Django secret key
  DB_PASSWORD: ""  # PostgreSQL password
  EMAIL_HOST_PASSWORD: ""  # Email service password
  AWS_ACCESS_KEY_ID: ""  # AWS access key (if using S3)
  AWS_SECRET_ACCESS_KEY: ""  # AWS secret key (if using S3)
  SENTRY_DSN: ""  # Sentry DSN for error tracking

---
# Template for postgres-secret
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: bugstracker-production
type: Opaque
stringData:
  database: ""
  username: ""
  password: ""

---
# Template for redis-secret
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: bugstracker-production
type: Opaque
stringData:
  password: ""

---
# Template for flower-secret
apiVersion: v1
kind: Secret
metadata:
  name: flower-secret
  namespace: bugstracker-production
type: Opaque
stringData:
  username: ""
  password: ""
